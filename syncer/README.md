# outernet-scrapbook-syncer

Installation:

```
cp .env.sample .env # Modify the config yourself
docker-compose up -d
```
Make sure you have a underpass tunnel running constantly, otherwise the whole syncer won't work.